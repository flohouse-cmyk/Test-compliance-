{
  "mcpServers": {
    "magic": {
      "command": "npx",
      "args": ["-y", "@21st-dev/magic@latest"],
      "env": { "API_KEY": "${MAGIC_API_KEY}" }
    },
    "pixelcut": {
      "type": "http",
      "url": "https://mcp.pixelcut.ai/mcp"
    }
  }
}
